const nextConfig = {
  assetPrefix: '', // Change to '' instead of '/ASP'
  basePath: '', // Change to '' instead of '/ASP'
  images: {
    unoptimized: true,
    loader: 'custom',
    loaderFile: './imageLoader.js',
  },
}
